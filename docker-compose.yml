services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: qdrant_db
    ports:
      - "6333:6333"
      - "6334:6334" # Untuk koneksi gRPC jika suatu saat perlu kecepatan ekstra
    volumes:
      - ./qdrant_db:/qdrant/storage
    restart: unless-stopped
